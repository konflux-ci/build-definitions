apiVersion: tekton.dev/v1
kind: Task
metadata:
  labels:
    app.kubernetes.io/version: "0.3"
  annotations:
    tekton.dev/pipelines.minVersion: "0.12.1"
    tekton.dev/tags: "konflux"
  name: init
spec:
  description: >-
    Initialize Pipeline Task, enables configuration for cache-proxy if required during the PipelineRun.
  params:
    - name: enable-cache-proxy
      description: Enable cache proxy configuration
      default: "false"
  results:
    - name: http-proxy
      description: HTTP proxy URL for cache proxy (when enable-cache-proxy is true)
    - name: no-proxy
      description: NO_PROXY value for cache proxy (when enable-cache-proxy is true)
  steps:
    - name: init
      image: quay.io/redhat-user-workloads/rhtap-build-tenant/konflux-build-cli:on-pr-cd0ef36d1914b30409b66575ce0b5a593d4788ed
      computeResources:
        limits:
          memory: 256Mi
          cpu: 100m
        requests:
          memory: 256Mi
          cpu: 100m
      env:
        - name: KBC_LOG_LEVEL
          value: "info"
      command: ["konflux-build-cli", "config", "cache-proxy"]
      args:
        - --enable
        - $(params.enable-cache-proxy)
