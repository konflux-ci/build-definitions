---
base: ../../push-dockerfile/0.2/push-dockerfile.yaml
add:
  - use-source
preferStepTemplate: true
removeWorkspaces:
  - workspace
replacements:
  workspaces.workspace.path: /var/workdir
description: |-
  Discover Dockerfile from source code and push it to registry as an OCI artifact.
