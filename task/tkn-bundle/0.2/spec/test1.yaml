apiVersion: tekton.dev/v1
kind: Task
metadata:
  labels:
    app.kubernetes.io/version: "0.1"
  name: test1
spec:
  params:
    - name: MY_PARAM
      type: string
      default: original-value
  steps:
    - name: build
      image: ubuntu
    - name: test
      image: alpine
