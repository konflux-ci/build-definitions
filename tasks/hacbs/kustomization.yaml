apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
patches:
  - path: network-isolation.yaml
    target:
      group: tekton.dev
      version: v1beta1
      kind: Task
      labelSelector: build-definition.network-isolated = yes
